# DECOMMISSIONED LEGACY GATEWAY
# Sensitive legacy routes are permanently disabled. This process exists only as
# a local tombstone while callers migrate to approved primary API operations.

PORT=3000

# Provider/database/Fabric credentials are intentionally omitted. Do not add
# live secrets to this gateway; migrate approved functions to the primary API.
