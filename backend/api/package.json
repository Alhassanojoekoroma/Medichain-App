{
  "name": "medichain-api-gateway",
  "version": "1.0.0",
  "description": "Local-only tombstone for the decommissioned MediChain legacy gateway",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
