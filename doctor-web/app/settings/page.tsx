'use client';
import { Settings } from 'lucide-react';
import { LayoutWrapper } from '@/components/dashboard/layout-wrapper';
import { useAuth } from '@/hooks/useAuth';
export default function SettingsPage() { const { user } = useAuth(); return <LayoutWrapper><section className="mx-auto max-w-3xl rounded-2xl border border-slate-200 bg-white p-6"><Settings className="h-7 w-7 text-slate-700" /><h1 className="mt-4 text-2xl font-bold text-slate-950">Account</h1><dl className="mt-5 grid gap-4 sm:grid-cols-2"><div><dt className="text-xs font-bold uppercase text-slate-400">Name</dt><dd className="mt-1 font-semibold text-slate-800">{user?.name || 'Unavailable'}</dd></div><div><dt className="text-xs font-bold uppercase text-slate-400">Email</dt><dd className="mt-1 font-semibold text-slate-800">{user?.email || 'Unavailable'}</dd></div><div><dt className="text-xs font-bold uppercase text-slate-400">Role</dt><dd className="mt-1 font-semibold capitalize text-slate-800">{user?.role || 'Unavailable'}</dd></div><div><dt className="text-xs font-bold uppercase text-slate-400">Hospital</dt><dd className="mt-1 font-semibold text-slate-800">{user?.hospital || 'Unavailable'}</dd></div></dl><p className="mt-6 rounded-xl bg-slate-50 p-4 text-sm text-slate-600">Profile changes must be made through the approved identity administration service. This portal does not simulate saving account changes.</p></section></LayoutWrapper>; }
