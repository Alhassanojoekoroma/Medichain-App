import React from 'react';
import { ScrollView, StyleSheet, Text, TouchableOpacity, View } from 'react-native';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { Ionicons } from '@expo/vector-icons';
import { StatusBar } from 'expo-status-bar';
import { Colors, FontSize, FontWeight, Radius, Shadow, Spacing } from '../theme';
import type { Record } from '../types';

function displayDate(value: string): string {
  const date = new Date(value);
  return Number.isNaN(date.getTime()) ? 'Date unavailable' : date.toLocaleDateString('en-SL', { dateStyle: 'medium' });
}

export default function RecordDetailScreen({ navigation, route }: any) {
  const insets = useSafeAreaInsets();
  const record = route?.params?.record as Record | undefined;
  if (!record) return <View style={[styles.emptyScreen, { paddingTop: insets.top + Spacing.xxl }]}><StatusBar style="dark" /><Ionicons name="document-text-outline" size={48} color={Colors.neutral400} /><Text style={styles.emptyTitle}>Record unavailable</Text><Text style={styles.emptyText}>Return to My records and select a confirmed record.</Text><TouchableOpacity style={styles.backButton} onPress={() => navigation.goBack()}><Text style={styles.backButtonText}>Back to records</Text></TouchableOpacity></View>;

  const shortHash = record.hash ? `${record.hash.slice(0, 16)}…` : null;
  return <View style={styles.screen}>
    <StatusBar style="dark" />
    <View style={[styles.header, { paddingTop: insets.top + Spacing.sm }]}><TouchableOpacity style={styles.iconButton} onPress={() => navigation.goBack()} accessibilityRole="button" accessibilityLabel="Go back"><Ionicons name="arrow-back" size={22} color={Colors.neutral900} /></TouchableOpacity><Text style={styles.headerTitle} numberOfLines={1}>Record details</Text><TouchableOpacity style={styles.iconButton} onPress={() => navigation.navigate('AccessLog')} accessibilityRole="button" accessibilityLabel="Open access log"><Ionicons name="time-outline" size={21} color={Colors.primary} /></TouchableOpacity></View>
    <ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}>
      <View style={styles.hero}><View style={styles.fileIcon}><Ionicons name="document-text" size={28} color={Colors.primary} /></View><Text style={styles.title}>{record.title}</Text><Text style={styles.subtitle}>{record.type} · {displayDate(record.date)}</Text>{record.hash ? <View style={styles.verified}><Ionicons name="shield-checkmark" size={15} color={Colors.successDark} /><Text style={styles.verifiedText}>Verified on-chain</Text></View> : <View style={styles.pending}><Ionicons name="time-outline" size={15} color={Colors.warningDark} /><Text style={styles.pendingText}>Integrity anchor pending</Text></View>}</View>
      <Section title="Record metadata"><Row label="Facility" value={record.hospital || 'Facility not listed'} /><Row label="Uploaded by" value={record.doctor || 'Provider not listed'} /><Row label="Record type" value={record.type || 'Not recorded'} /><Row label="Created" value={displayDate(record.date)} /></Section>
      {record.supersedes ? <Section title="Correction history"><Text style={styles.correctionText}>This record corrects an earlier version. The earlier record remains preserved in the audit trail.</Text><Text style={styles.referenceText}>Supersedes: {record.supersedes}</Text></Section> : null}
      <Section title="Integrity"><Text style={styles.integrityText}>{record.hash ? 'This record has a server-confirmed integrity anchor. The full hash is not exposed as a permanent file link.' : 'The record is available, but its integrity anchor has not yet been confirmed.'}</Text>{shortHash ? <View style={styles.hash}><Ionicons name="link-outline" size={16} color={Colors.primary} /><Text style={styles.hashText} selectable>{shortHash}</Text></View> : null}</Section>
      <TouchableOpacity style={styles.accessButton} onPress={() => navigation.navigate('AccessLog')} accessibilityRole="button"><Ionicons name="eye-outline" size={19} color={Colors.primary} /><Text style={styles.accessButtonText}>View access history</Text></TouchableOpacity>
    </ScrollView>
  </View>;
}

function Section({ title, children }: { title: string; children: React.ReactNode }) { return <View style={styles.card}><Text style={styles.cardTitle}>{title}</Text>{children}</View>; }
function Row({ label, value }: { label: string; value: string }) { return <View style={styles.row}><Text style={styles.rowLabel}>{label}</Text><Text style={styles.rowValue}>{value}</Text></View>; }

const styles = StyleSheet.create({
  screen: { flex: 1, backgroundColor: Colors.bg }, header: { flexDirection: 'row', alignItems: 'center', gap: Spacing.md, paddingHorizontal: Spacing.lg, paddingBottom: Spacing.md, borderBottomWidth: 1, borderBottomColor: Colors.border, backgroundColor: Colors.white }, iconButton: { width: 44, height: 44, borderRadius: Radius.pill, alignItems: 'center', justifyContent: 'center', backgroundColor: Colors.primaryLight }, headerTitle: { flex: 1, color: Colors.neutral900, fontSize: FontSize.h4, fontWeight: FontWeight.bold, textAlign: 'center' }, content: { padding: Spacing.lg, paddingBottom: Spacing.xxxl, gap: Spacing.md }, hero: { alignItems: 'center', padding: Spacing.xl, borderRadius: Radius.lg, borderWidth: 1, borderColor: Colors.border, backgroundColor: Colors.white, ...Shadow.card }, fileIcon: { width: 58, height: 58, borderRadius: Radius.md, alignItems: 'center', justifyContent: 'center', backgroundColor: Colors.primaryLight }, title: { marginTop: Spacing.md, color: Colors.neutral900, fontSize: FontSize.h2, fontWeight: FontWeight.bold, textAlign: 'center' }, subtitle: { marginTop: Spacing.xs, color: Colors.textMuted, fontSize: FontSize.bodySmall, textAlign: 'center' }, verified: { flexDirection: 'row', alignItems: 'center', gap: Spacing.xs, marginTop: Spacing.md, paddingHorizontal: Spacing.md, paddingVertical: Spacing.sm, borderRadius: Radius.pill, backgroundColor: Colors.successLight }, verifiedText: { color: Colors.successDark, fontSize: FontSize.caption, fontWeight: FontWeight.bold }, pending: { flexDirection: 'row', alignItems: 'center', gap: Spacing.xs, marginTop: Spacing.md, paddingHorizontal: Spacing.md, paddingVertical: Spacing.sm, borderRadius: Radius.pill, backgroundColor: Colors.warningLight }, pendingText: { color: Colors.warningDark, fontSize: FontSize.caption, fontWeight: FontWeight.bold }, card: { padding: Spacing.lg, borderRadius: Radius.lg, borderWidth: 1, borderColor: Colors.border, backgroundColor: Colors.white, ...Shadow.card }, cardTitle: { marginBottom: Spacing.sm, color: Colors.neutral900, fontSize: FontSize.h4, fontWeight: FontWeight.bold }, row: { flexDirection: 'row', justifyContent: 'space-between', gap: Spacing.md, paddingVertical: Spacing.sm, borderTopWidth: 1, borderTopColor: Colors.neutral100 }, rowLabel: { color: Colors.textMuted, fontSize: FontSize.caption }, rowValue: { flex: 1, color: Colors.neutral900, fontSize: FontSize.caption, fontWeight: FontWeight.medium, textAlign: 'right' }, correctionText: { color: Colors.textBody, fontSize: FontSize.bodySmall, lineHeight: 19 }, referenceText: { marginTop: Spacing.sm, color: Colors.textMuted, fontSize: FontSize.caption }, integrityText: { color: Colors.textBody, fontSize: FontSize.bodySmall, lineHeight: 19 }, hash: { flexDirection: 'row', alignItems: 'center', gap: Spacing.sm, marginTop: Spacing.md, padding: Spacing.md, borderRadius: Radius.md, backgroundColor: Colors.primaryLight }, hashText: { flex: 1, color: Colors.primaryDark, fontSize: FontSize.caption, fontFamily: 'monospace' }, accessButton: { minHeight: 48, flexDirection: 'row', alignItems: 'center', justifyContent: 'center', gap: Spacing.sm, borderRadius: Radius.pill, borderWidth: 1, borderColor: Colors.primaryMid, backgroundColor: Colors.white }, accessButtonText: { color: Colors.primary, fontSize: FontSize.body, fontWeight: FontWeight.bold }, emptyScreen: { flex: 1, alignItems: 'center', paddingHorizontal: Spacing.xxl, backgroundColor: Colors.bg }, emptyTitle: { marginTop: Spacing.md, color: Colors.neutral900, fontSize: FontSize.h3, fontWeight: FontWeight.bold }, emptyText: { marginTop: Spacing.sm, color: Colors.textMuted, fontSize: FontSize.bodySmall, textAlign: 'center' }, backButton: { minHeight: 48, marginTop: Spacing.xl, paddingHorizontal: Spacing.xl, justifyContent: 'center', borderRadius: Radius.pill, backgroundColor: Colors.primary }, backButtonText: { color: Colors.white, fontWeight: FontWeight.bold },
});
