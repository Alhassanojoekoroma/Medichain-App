import React, { useMemo, useState } from 'react';
import { RefreshControl, ScrollView, StyleSheet, Text, TouchableOpacity, View } from 'react-native';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { Ionicons } from '@expo/vector-icons';
import { StatusBar } from 'expo-status-bar';
import { Colors, FontSize, FontWeight, Radius, Shadow, Spacing } from '../theme';
import { useStore } from '../store/useStore';

function formatDate(value: string): string {
  const date = new Date(value);
  return Number.isNaN(date.getTime()) ? 'Date unavailable' : date.toLocaleDateString('en-SL', { day: '2-digit', month: 'short', year: 'numeric' });
}

export default function RecordsScreen({ navigation }: any) {
  const insets = useSafeAreaInsets();
  const { records, syncStatus, syncError, lastSyncedAt, syncPatientData } = useStore();
  const types = useMemo(() => ['All', ...Array.from(new Set(records.map(record => record.type).filter(Boolean)))], [records]);
  const [selectedType, setSelectedType] = useState('All');
  const filtered = selectedType === 'All' ? records : records.filter(record => record.type === selectedType);
  const syncText = lastSyncedAt ? `Last updated ${formatDate(lastSyncedAt)}` : 'Not synced yet';

  return <View style={styles.screen}>
    <StatusBar style="dark" />
    <View style={[styles.header, { paddingTop: insets.top + Spacing.md }]}>
      <View><Text style={styles.title}>My records</Text><Text style={styles.subtitle}>Only records confirmed by your account are shown.</Text></View>
      <TouchableOpacity style={styles.headerAction} onPress={() => navigation.navigate('AccessLog')} accessibilityRole="button" accessibilityLabel="View record access log"><Ionicons name="time-outline" size={21} color={Colors.primary} /></TouchableOpacity>
    </View>
    <View style={styles.syncRow}><View style={[styles.syncDot, syncStatus === 'error' && styles.syncDotWarning]} /><Text style={styles.syncText}>{syncStatus === 'syncing' ? 'Updating securely…' : syncText}</Text></View>
    {syncStatus === 'error' ? <TouchableOpacity style={styles.errorBanner} onPress={() => void syncPatientData()} accessibilityRole="button"><Ionicons name="cloud-offline-outline" size={18} color={Colors.warningDark} /><Text style={styles.errorText}>{syncError || 'Connection unavailable. Showing last-synced records.'}</Text><Text style={styles.retry}>Retry</Text></TouchableOpacity> : null}
    {types.length > 1 ? <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.filterList}>{types.map(type => <TouchableOpacity key={type} style={[styles.filter, selectedType === type && styles.filterActive]} onPress={() => setSelectedType(type)} accessibilityRole="tab" accessibilityState={{ selected: selectedType === type }}><Text style={[styles.filterText, selectedType === type && styles.filterTextActive]}>{type}</Text></TouchableOpacity>)}</ScrollView> : null}
    <ScrollView style={styles.list} contentContainerStyle={styles.listContent} refreshControl={<RefreshControl refreshing={syncStatus === 'syncing'} onRefresh={() => void syncPatientData()} tintColor={Colors.primary} />}>
      {filtered.length === 0 ? <View style={styles.empty}><Ionicons name="folder-open-outline" size={48} color={Colors.neutral400} /><Text style={styles.emptyTitle}>{records.length ? 'No records in this category' : 'No records yet'}</Text><Text style={styles.emptyText}>{records.length ? 'Choose another category to see your active records.' : 'A verified clinician’s upload will appear with its real processing state.'}</Text></View> : filtered.map(record => <TouchableOpacity key={record.id} style={styles.card} onPress={() => navigation.navigate('RecordDetail', { record })} accessibilityRole="button" accessibilityLabel={`Open ${record.title}`}><View style={styles.icon}><Ionicons name="document-text-outline" size={22} color={Colors.primary} /></View><View style={styles.copy}><Text style={styles.recordTitle} numberOfLines={1}>{record.title}</Text><Text style={styles.recordFacility} numberOfLines={1}>{record.hospital || 'Facility not listed'}</Text><Text style={styles.recordMeta}>{record.type} · {formatDate(record.date)}</Text></View>{record.hash ? <View style={styles.verified}><Ionicons name="shield-checkmark" size={15} color={Colors.successDark} /><Text style={styles.verifiedText}>On-chain</Text></View> : <View style={styles.pending}><Text style={styles.pendingText}>Anchor pending</Text></View>}</TouchableOpacity>)}
      <View style={{ height: Spacing.xxl }} />
    </ScrollView>
  </View>;
}

const styles = StyleSheet.create({
  screen: { flex: 1, backgroundColor: Colors.bg }, header: { flexDirection: 'row', alignItems: 'flex-start', justifyContent: 'space-between', paddingHorizontal: Spacing.lg, paddingBottom: Spacing.md, backgroundColor: Colors.white, borderBottomWidth: 1, borderBottomColor: Colors.border }, title: { color: Colors.neutral900, fontSize: FontSize.h2, fontWeight: FontWeight.bold }, subtitle: { marginTop: 3, color: Colors.textMuted, fontSize: FontSize.caption }, headerAction: { width: 44, height: 44, borderRadius: Radius.pill, alignItems: 'center', justifyContent: 'center', backgroundColor: Colors.primaryLight }, syncRow: { flexDirection: 'row', alignItems: 'center', gap: Spacing.sm, paddingHorizontal: Spacing.lg, paddingVertical: Spacing.sm, backgroundColor: Colors.bg }, syncDot: { width: 7, height: 7, borderRadius: Radius.pill, backgroundColor: Colors.success }, syncDotWarning: { backgroundColor: Colors.warning }, syncText: { color: Colors.textMuted, fontSize: FontSize.caption }, errorBanner: { flexDirection: 'row', alignItems: 'center', gap: Spacing.sm, marginHorizontal: Spacing.lg, marginBottom: Spacing.sm, padding: Spacing.md, borderRadius: Radius.md, borderWidth: 1, borderColor: Colors.warningBorder, backgroundColor: Colors.warningLight }, errorText: { flex: 1, color: Colors.warningDark, fontSize: FontSize.caption, lineHeight: 17 }, retry: { color: Colors.warningDark, fontWeight: FontWeight.bold, fontSize: FontSize.caption }, filterList: { gap: Spacing.sm, paddingHorizontal: Spacing.lg, paddingVertical: Spacing.sm }, filter: { minHeight: 40, justifyContent: 'center', paddingHorizontal: Spacing.md, borderRadius: Radius.pill, backgroundColor: Colors.white, borderWidth: 1, borderColor: Colors.border }, filterActive: { backgroundColor: Colors.primary, borderColor: Colors.primary }, filterText: { color: Colors.textMuted, fontSize: FontSize.bodySmall, fontWeight: FontWeight.medium }, filterTextActive: { color: Colors.white }, list: { flex: 1 }, listContent: { padding: Spacing.lg, gap: Spacing.sm }, card: { flexDirection: 'row', alignItems: 'center', gap: Spacing.md, padding: Spacing.lg, borderRadius: Radius.lg, borderWidth: 1, borderColor: Colors.border, backgroundColor: Colors.white, ...Shadow.card }, icon: { width: 44, height: 44, borderRadius: Radius.md, alignItems: 'center', justifyContent: 'center', backgroundColor: Colors.primaryLight }, copy: { flex: 1, minWidth: 0 }, recordTitle: { color: Colors.neutral900, fontSize: FontSize.bodySmall, fontWeight: FontWeight.bold }, recordFacility: { marginTop: 2, color: Colors.textBody, fontSize: FontSize.caption }, recordMeta: { marginTop: 2, color: Colors.textMuted, fontSize: FontSize.label }, verified: { alignItems: 'center', gap: 2, paddingHorizontal: Spacing.sm, paddingVertical: 5, borderRadius: Radius.pill, backgroundColor: Colors.successLight }, verifiedText: { color: Colors.successDark, fontSize: FontSize.label, fontWeight: FontWeight.bold }, pending: { paddingHorizontal: Spacing.sm, paddingVertical: 5, borderRadius: Radius.pill, backgroundColor: Colors.warningLight }, pendingText: { color: Colors.warningDark, fontSize: FontSize.label, fontWeight: FontWeight.medium }, empty: { alignItems: 'center', paddingTop: Spacing.xxxl, paddingHorizontal: Spacing.xxl }, emptyTitle: { marginTop: Spacing.md, color: Colors.neutral900, fontSize: FontSize.h4, fontWeight: FontWeight.bold }, emptyText: { marginTop: Spacing.sm, color: Colors.textMuted, textAlign: 'center', fontSize: FontSize.bodySmall, lineHeight: 19 },
});
